// this file is deprecated
const fs = require('fs');
const { Window } = require('node-screenshots');

const windows = Window.all();
const target = windows.find((win) => win.title === 'MapleStory');

if (target) {
  const image = target.captureImageSync();
  fs.writeFileSync('maple.png', image.toPngSync());
}
